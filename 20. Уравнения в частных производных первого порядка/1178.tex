\documentclass[a5paper,10pt]{article}

% https://github.com/InDevRus/filippov-solutions

\input{../preambles/general}

\begin{document}
    $\pow{x}{2} z \parder{z} {x} + \pow{y}{2} z \parder{z} {y} = x + y$.
    $\dfrac {dx} {\pow{x}{2} z} = \dfrac {dy} {\pow{y}{2} z} = \dfrac {dz} {x + y}$.

    Первые две дроби сразу являются интегрируемой комбинацией:
    $\dfrac {dx} {\pow{x}{2}} = \dfrac {dy} {\pow{y}{2}}$.
    $\dfrac {1} {x} + \dfrac {1} {y} = \linebreak = \dfrac {x + y} {xy} = A$.

    Вторую интегрируемую комбинацию из системы
    $\dfrac {dx} {\pow{x}{2}} = \dfrac {dy} {\pow{y}{2}} = \dfrac {z dz} {x + y}$
    можно получить, например, сравняв знаменатели по свойству дробей:
    $\dfrac {z dz} {x + y} = \dfrac {\frac {1} {x} dx + \frac {1} {y} dy} {\frac {1} {x} \cdot \pow{x}{2} + \frac {1} {y} \cdot \pow{y}{2}} = \dfrac {d\ln\vbr{xy}} {x + y}$.
    Итак,
    $z dz - d\ln\vbr{xy} = 0$, откуда
    $xy \tpow{e}{-\frac {1} {2} \pow{z}{2}} = B$.

    Общее решение в неявной форме будет иметь вид
    $F\br{\dfrac {x + y} {xy}; xy \tpow{e}{-\frac {1} {2} \pow{z}{2}}} = 0$.
    В явной же форме получаем
    $\pow{z}{2} = f\br{\dfrac {x + y} {xy}} - \ln\br{\pow{x}{2} \pow{y}{2}}$.
\end{document}
