\documentclass[a5paper,10pt]{article}

% https://github.com/InDevRus/filippov-solutions

\input{../preambles/general}

\begin{document}
    $y \parder{z} {x} + x \parder{z} {y} = x - y$. \vspace{1mm}

    $\dfrac {dx} {y} = \dfrac {dy} {x} = \dfrac {dz} {x - y}$. \vspace{1mm}

    \begin{enumerate}
        \item $\dfrac {dx} {y} = \dfrac {dy} {x}$; $x dx - y dy = 0$; $\pow{x}{2} + \pow{y}{2} = A$.

        \item Следующую интегрируемую комбинацию получим из первых двух дробей:
        $\dfrac {dz} {x - y} \linebreak = \dfrac {dy - dx} {x - y}$.
        $dz = d\br{y - x}$.
        $z - y + x = B$.
    \end{enumerate}

    Итак, получаем в неявном виде
    $F\br{\pow{x}{2} + \pow{y}{2}; z - y + x} = 0$,
    откуда можно также выразить решение в явном виде
    $z - y + x = f\br{\pow{x}{2} + \pow{y}{2}}$;
    $z\br{x; y} = y - x + f\br{\pow{x}{2} + \pow{y}{2}}$.
\end{document}
